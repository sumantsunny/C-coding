#include<stdio.h>

   void main()
   {
      // int x;
      int y;
      // printf("Enter the string");
      // scanf("%c", &x);
      y = printf("Sumant Kumar");          //It also includes space
      printf("the length of the string is : %d",y);
   }